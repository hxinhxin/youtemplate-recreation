// Dedicated palette for this trailer — black/red/white per the brief.
// Separate from concert-promo/theme.ts (pink) so the other two videos
// keep their existing branding untouched.
export const theme = {
  background: "#050505",
  red: "#e2102b",
  redDeep: "#7a0714",
  white: "#ffffff",
  textMuted: "rgba(255,255,255,0.7)",
  headlineFont: "'Arial Black', 'Helvetica Neue', Arial, sans-serif",
  bodyFont: "'Helvetica Neue', Arial, sans-serif",
};
