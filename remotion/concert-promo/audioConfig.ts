export const audioConfig = {
  src: "audio/track.mp3",
  // Seconds into the track to start playing from. Defaults to the top of
  // the song — tell me a timestamp (e.g. the drop) if you want a
  // different section used instead.
  startFromSeconds: 0,
  fadeInFrames: 15,
  fadeOutFrames: 30,
};
