---
description: List the Blender skills available in this repo and what each one covers
allowed-tools: Bash, Read, Glob
---

List what is installed under `.claude/skills/`:

1. The eight pipeline skills authored for this repo (`toon-pipeline`, `toon-character`,
   `toon-rig`, `snap-animation`, `toon-camera`, `toon-materials`, `toon-style`,
   `toon-render`) — say what each is for in one line.
2. The vendored `blender-skills` pack (arjun988/blender-skills, MIT) — group the
   ~95 skills by theme rather than listing them all: modeling, styles, worlds,
   genres, pipeline/export, VFX.
3. Then list the slash commands in `.claude/commands/` with their descriptions.
